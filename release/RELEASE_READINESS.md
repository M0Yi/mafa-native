# 收尾交付状态

当前仍为收尾中的单机预发布，未达到 C00–C19 全部完成条件。此页用于快速定位剩余工作，详细证据以 [收尾计划](CLOSEOUT_PLAN.md)、[任务清单](closeout-tasks.json) 和各次原始报告为准。

## 当前可确认

- 操作页面已完成多轮代码修复：窗口生命周期、物品拖拽与拆分、快捷栏兼容、选角和保存失败反馈等。
- 最近整组代码回归 39 组通过，测试期间源码指纹未变化，见 `artifacts/ui-closeout-all/report.json`。覆盖页面、物品事务及资源损坏处理；报告单独保留退出资源警告。功能通过不等于实际输入、视觉、性能或三平台验收通过。
- 本地可达 Git 历史检查未发现脚本覆盖的禁止路径、SQLite/WAV/M211 特征；不等于图片来源和许可核查完成。
- 当前源码修复未重新打包，下载页的预览包不自动包含这些修改。

## 交付前仍需关闭的缺口

| 范围 | 仍需完成 | 不能替代它的证据 |
| --- | --- | --- |
| 导入与缓存 | 应用界面强退恢复、完整导入界面的补充阶段中断恢复及多应用共享设置 | 已通过基础及独立补充转换子进程中断重试、缓存校验中断及双校验进程检查；尚不覆盖这些完整界面流程 |
| 操作页面 | 必要的真实拖拽、输入法、典型窗口缩放与点击验证 | 直接调用回调和注入事件 |
| 音画 | 困魔咒/地狱火/地狱雷光原帧、火符飞行及安全区原版标记；缺失技能阶段声音、关键动作听感与时序、代表场景贴图 | 音频可解码、资源索引存在 |
| 退出资源 | 仍存在音频相关退出资源释放警告，详见 AUDIO_EXIT_DIAGNOSIS.md | 功能回归通过 |
| 实体手柄 | 连接、摇杆、肩键切页、确认返回完整流程 | 合成 Joypad 事件；最近设备探测为空 |
| 三平台与性能 | 当前源码 Windows/Linux 图形实机核心流程；Mac 两小时前台探索及独立大规模战斗、外部 RSS | 历史 CI 构建、无界面运行、静态内存统计 |
| 最终交付 | 素材与宣传来源人工核查，获授权后正式归档及归档内容复核 | 当前源码允许清单 |

地图按用户确认的收尾范围抽查：新手村、商铺、传送操作涉及的场景及已知石墓卡点，不安排全部地图逐张人工验收。

## 开发与数据保护

继续使用项目根目录 `启动最新开发版.command`。日常不升级发布号、不导出安装包；不修改客户端或用户存档。测试使用独立临时数据。尚无实体手柄和 Windows/Linux 测试设备确认信息，设备相关验收保持待办；其余可独立开展的工作继续推进。

实际输入与设备验证按 [最少实机操作验收](MANUAL_ACCEPTANCE.md) 合并执行，避免重复逐页操作；该表已记录当前默认窗口的部分实际键鼠操作，其余项仍待验收。

## 最近原生长测

本轮有效时间 2695.422 秒后提前结束，退出码 0，未生成完成报告，原因未确定。539 条五秒间隔采样中有 8 条低于 60 FPS，最低 49 FPS；不是逐帧卡顿统计。两小时及稳定 60 FPS 均未通过验收。原始记录保留，汇总见 `artifacts/closeout-native-soak-partial-analysis.json`。下一轮记录器已增加当前地图和退出生命周期字段；这些字段无法反推旧记录的退出原因。

第二轮长测已结束，驱动三小时墙钟上限到达，结果文件 `artifacts/closeout-native-soak-v2-result.json` 明确 duration_reached=false：有效 2667.855 秒、8 次程序化切图、1762 次位置变化、资源错误目录为空。失焦后的暂停时间未计入。日志含 recorder_exit_tree，执行会话已不存在，退出码无法回取。不满足两小时门槛，也不代表战斗、稳定帧率或内存验收通过。未自动重启长测。
