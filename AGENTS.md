# 本项目开发约定

用户已明确要求：日常开发不逐次编译安装版。

- 默认只修改源码、运行开发入口、执行与变更有关的测试。
- 固定启动入口：项目根目录 `启动最新开发版.command`。
- 不在每次修改后增加发布版本号、导出 `.app`、生成安装或源码 ZIP、复制到 Applications。
- 只有用户明确要求打包、发布或安装时才执行相应操作。构建脚本使用 `--release`，安装另加 `--install`；参数不替代用户授权。
- 每次开发结束清理本次产生的失败构建、临时构建目录和重复开发归档；不删除工程结构或源码文件。清理开发过程中创建的失败构建和临时副本；不要删除源码、客户端素材、用户存档、Godot 运行环境。不要自动删除用户需要保留的正式发布成果。
- 最近一个旧安装版只作备用，当前功能以源码开发入口为准。
